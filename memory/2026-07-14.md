# 2026-07-14 — Session Log

## Project: SpecDriven Platform (monorepo)
**Path**: `D:\Aceleradores\specdriven-platform`
**Structure**: `apps/api`, `apps/web-client`, `apps/web-staff`, `packages/shared`

---

## Completed Features

### 1. CORS Fix
- Added `credentials: true` to `@fastify/cors` config in `apps/api/src/index.ts` (~line 208)
- Fixed "Falha ao conectar na API" on login

### 2. Mail Provider Switch
- Changed `.env` from Brevo SMTP to Mailpit local (`SMTP_HOST=127.0.0.1`, `SMTP_PORT=1025`)
- Brevo rejected unverified sender `iagolimastx@gmail.com`

### 3. CSRF Fix
- Rewrote `resolveAuthUser()` in `apps/api/src/auth.ts` (line ~425)
- Checks `Authorization: Bearer` header BEFORE cookie — prevents `forbidden_csrf_invalid`

### 4. Permission Fix
- Added `gestor` role to `canManageProjects()` in `apps/api/src/permissions.ts`

### 5. Sidebar Nav
- Added "Projetos" link in `apps/web-staff/src/components/AppShell.tsx` between "Clientes" and "Relatórios"

### 6. Client Portal NewTicketPage
- Rewrote `apps/web-client/src/pages/NewTicketPage.tsx`
- Removed `companyName` field and `listClients` import
- Projects auto-load from `user.clientId`

### 7. Staff ClientsPage Restructure
- Rewrote `apps/web-staff/src/pages/ClientsPage.tsx` (note: this was rewritten to web-staff, NOT web-client despite path in summary — verify)
- Expandable client cards: Projects table, Users table, Pending Invites per client
- Info banner about automatic Client→Projects→Users linkage

---

## In Progress: UserProjects (User↔Project Many-to-Many)

### Done
- **Schema**: `UserProject` model added to `schema.prisma` with `userId`, `projectId`, `active`, `createdAt`, `updatedAt`
- **Relations**: `userProjects UserProject[]` added to both `Project` and `User` models
- **DB**: `user_projects` table created via `npx prisma db push`
- **API Handler**: `apps/api/src/user-projects.ts` created with:
  - `GET /user-projects?userId=xxx&projectId=xxx` — list active links
  - `POST /projects/:id/users` — link user to project (reactivates if exists)
  - `DELETE /projects/:id/users` — unlink (sets active=false)
- **Routes registered** in `apps/api/src/index.ts`
- **Staff portal API functions** added for user-projects
- **Client portal logic**: filters projects by user-projects if links exist

### ⚠️ CRITICAL ISSUE — Schema was partially overwritten
When I wrote the full schema to `schema.prisma`, I accidentally **removed fields** that the codebase depends on:
- **Project model**: missing `billingModel`, `baselineHoursMonth`, `hourlyRateCents`, `ticketRateCents`, `budgetCents`, `startDate`, `endDate`, `slaActiveStatuses` (expected by `ProjectSchema` in shared and `ProjectsSettingsPage.tsx` and `seed.ts`)
- **Ticket model**: missing `projectId` field and `project` relation (expected by `TicketSchema` and `seed.ts`)
- **Ticket model**: missing `csatScore`, `csatComment` (from TicketSchema but may not be in DB yet — check)

### What needs to be restored in schema.prisma

**Project model** needs these fields added back:
```
billingModel       String   @default("per_hour")
baselineHoursMonth Float?
hourlyRateCents    Int?
ticketRateCents    Int?
budgetCents        Int?
startDate          DateTime?
endDate            DateTime?
slaActiveStatuses  String   @default("em_andamento")
```

**Ticket model** needs:
```
projectId String?  (field)
project   Project? @relation(fields: [projectId], references: [id], onDelete: SetNull)
```
And `@@index([projectId])` added.

### After schema fix
1. Run `npx prisma db push` (in `apps/api`) to sync
2. Run `npx prisma generate` to regenerate client
3. Run `npx tsx prisma/seed.ts` to repopulate data
4. Kill and restart API: `npx tsx watch src/index.ts` in `apps/api`
5. Verify staff portal at http://localhost:5174

### Staff portal `ProjectsSettingsPage.tsx` (`apps/web-staff/src/pages/settings/ProjectsSettingsPage.tsx`)
- Already exists with full CRUD, billing model, module assignment (N1/N2/N3)
- Has form fields for billingModel, baselineHoursMonth, hourlyRate, ticketRate, budget, startDate, endDate
- Uses `selectProjectForEdit()` that reads all those fields from project
- Will break if Project model doesn't have those fields (API returns them as undefined)

### Seed file (`apps/api/prisma/seed.ts`)
- References `billingModel: "per_hour"` and `projectId: project.id` on upserts
- Will fail if those fields are missing from schema

---

## Seed Users
| Email | Password | Role | clientId |
|-------|----------|------|----------|
| master@blendit.local | changeme | master | null |
| admin@blendit.local | changeme | admin | null |
| gestor@specdriven.local | changeme | gestor | null |
| consultor@specdriven.local | changeme | consultor | null |
| cliente@specdriven.local | changeme | cliente | 00000000-0000-4000-8000-000000000002 |

## Ports
- API: 3000
- Client portal: 5173
- Staff portal: 5174
- Postgres: 5432
- Mailpit: 1025 (SMTP) / 8025 (web)
- MinIO: 9000 / 9001

## Key Technical Notes
- **Write tool** sometimes fails with "sandbox" error but worked for some files — use `exec` with PowerShell as fallback
- **PowerShell string escaping** is unreliable for complex code — use here-strings `@'...'@` or `[System.IO.File]::WriteAllText()`
- `npx tsx watch` auto-reloads `.ts` changes but `.env` changes need manual restart
- **Docker services**: Postgres, Mailpit, MinIO must be running
